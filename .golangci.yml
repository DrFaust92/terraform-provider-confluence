# Visit https://golangci-lint.run/ for usage documentation.
version: "2"
linters:
  default: standard
  settings:
    staticcheck:
      # Allow legacy initialisms such as Id/Url (was golint's "should be ID").
      checks:
        - all
        - -ST1003
formatters:
  enable:
    - gofmt
